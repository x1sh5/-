<view class="t-class loading-content {{position}}" style="{{(position === 'static' || noMask) ? 'visibility: hidden;' : ''}} {{'background-color: ' + backgroundColor}}">
  <t-loading class="loading" theme="{{type}}" layout="{{vertical}}" size="{{size}}">
    <slot></slot>
  </t-loading>
</view>
